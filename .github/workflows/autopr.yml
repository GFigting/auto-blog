name: Auto PR from stage to main

on:
  push:
    branches: [stage]

permissions:
  contents: write
  pull-requests: write

env:
  GH_TOKEN: ${{ secrets.GH_TOKEN || github.token }}

jobs:
  create-pull-request:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Ensure 'autopr' label exists
        run: |
          gh label create autopr \
            --description "Automated PR from stage to main" \
            --color "#006b75" 2>/dev/null || true

      - name: Create or update PR
        id: pr
        run: |
          # 检查是否存在从 stage 到 main 的 PR
          EXISTING_PR=$(gh pr list --base main --head stage | head -n 1 | awk '{print $1}' | sed 's/#//')

          if [ -z "$EXISTING_PR" ]; then
            # 创建新 PR 并获取 PR 编号
            PR_URL=$(gh pr create \
              --base main \
              --head stage \
              --title "Auto PR from stage to main" \
              --body "Automated PR from stage to main branch." \
              --label autopr)
            PR_NUMBER=$(echo "$PR_URL" | awk -F'/' '{print $NF}')
          else
            # 使用现有 PR 编号
            PR_NUMBER=$EXISTING_PR
          fi

          echo "number=$PR_NUMBER" >> $GITHUB_OUTPUT

      - name: Request review
        if: steps.pr.outputs.number != ''
        uses: necojackarc/auto-request-review@v0.12.0
        with:
          token: ${{ env.GH_TOKEN }}
          number: ${{ steps.pr.outputs.number }}

      - name: Auto-merge PR
        if: steps.pr.outputs.number != ''
        uses: ridedott/merge-me-action@v2
        with:
          github-token: ${{ env.GH_TOKEN }}
          pull-request-number: ${{ steps.pr.outputs.number }}
          merge-method: squash
          commit-title: "Auto-merged from stage to main"
          commit-message: "Automatically merged from stage branch."
          delete-branch: true
